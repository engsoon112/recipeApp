package com.base.ecomm.data

enum class StatusAPI {
    SUCCESS,
    ERROR,
    FAILED,
    LOADING,
    TOKEN_EXPIRED,
    UNAUTHORIZED,
    MAINTENANCE,
    INVALID_PARAMETER,
    VERSION_UPDATE,
    NETWORK_ERROR
}
